# LabelGuard OCR & PDF Dependencies for Linux (Render) & Windows
# PaddlePaddle CPU wheel
paddlepaddle>=3.0.0
paddleocr>=3.0.0

# OpenCV headless ensures no missing libGL.so.1 errors on Render / headless Linux
opencv-python-headless>=4.10.0.84

# Image processing & PDF generation
Pillow>=10.0.0
reportlab>=4.0.0

# Geometrical and numerical processing
shapely>=2.0.0
pyclipper>=1.3.0
numpy>=1.24.0

